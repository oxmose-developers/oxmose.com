---
import { sanityClient } from "sanity:client";
import { FAQsQuery } from "../../groq";

const faqs = await sanityClient.fetch<FAQsQuery>(FAQsQuery);

const firstFaq = faqs[0];

return Astro.redirect(`/faq/${firstFaq.slug.current}`);
---
